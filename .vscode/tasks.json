{
    "version": "2.0.0",
    "type": "shell",
    "args": [
        "${fileBasename}"
    ],
    "windows": {
        "command": "${workspaceRoot}\\pawno\\pawncc.exe",
        "options": {
            "cwd": "${fileDirname}"
        }
    },
    "group": {
        "kind": "build",
        "isDefault": true
    }
}